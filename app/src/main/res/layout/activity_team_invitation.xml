<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".TeamInvitationActivity">

    <TextView
        android:id="@+id/invite_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="56dp"
        android:layout_marginEnd="56dp"
        android:text="<Team Invitation Text>"
        android:textColor="@color/white"
        android:textSize="25sp"
        android:translationZ="1sp"
        app:layout_constraintBottom_toBottomOf="@id/invite_text_bg"
        app:layout_constraintEnd_toEndOf="@id/invite_text_bg"
        app:layout_constraintStart_toStartOf="@+id/invite_text_bg"
        app:layout_constraintTop_toTopOf="@id/invite_text_bg" />

    <ImageView
        android:id="@+id/invite_text_bg"
        android:layout_width="0dp"
        android:layout_height="176dp"
        android:layout_marginStart="56dp"
        android:layout_marginEnd="56dp"
        app:srcCompat="@drawable/rounded_rectangle"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintVertical_bias="0.25"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent">

        <Button
            android:id="@+id/accept_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="12pt"
            android:text="@android:string/ok" />

        <Button
            android:id="@+id/cancel_button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:textSize="12pt"
            android:text="@android:string/no" />
    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>